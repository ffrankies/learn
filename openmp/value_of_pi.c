/*
 * Estimating the value of Pi using the circle-in-square method.
 */
# include <stdlib.h>
# include <stdio.h>
# include <limits.h>

int main(int argc, char* argv[]) {
    srandom(1995);  // Seed the random number generator
    int n_attempts = INT_MAX;
    printf("Number of attemps = %d\n", n_attempts);
}